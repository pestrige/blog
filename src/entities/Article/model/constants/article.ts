export enum ArticleSort {
	VIEWS = "views",
	TITLE = "title",
	CREATED = "createdAt",
}

export enum ArticleBlockType {
	CODE = "CODE",
	IMAGE = "IMAGE",
	TEXT = "TEXT",
}

export enum ArticleType {
	ALL = "ALL",
	IT = "IT",
	SCIENCE = "SCIENCE",
	ECONOMICS = "ECONOMICS",
}

export enum ArticleView {
	LIST = "LIST",
	GRID = "GRID",
}
